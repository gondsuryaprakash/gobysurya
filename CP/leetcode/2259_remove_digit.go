package leetcode

func RemoveDigit(number string, digit byte) string {

	// maxNumber := -1

	// for i := 0; i < len(number); i++ {

	// 	if number[i] == digit {
	// 		copy:= number

	// 	}

	// }

	/*

		case 0 - if occurance of the digit at only once thne remove form string
		case 1 : checks digit present at what places
		case 2 :


		    conditions - If at rightmosst need to check left is greater then or not

	*/
	return ""
}
